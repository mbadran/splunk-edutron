import React from "react";
import { APP_VERSION } from "@/utils/constants";

interface FooterProps {
  isHomepage?: boolean;
}

const Footer: React.FC<FooterProps> = ({ isHomepage = false }) => {
  const backgroundClass = isHomepage
    ? "bg-white/20 backdrop-blur-md border-white/10"
    : "bg-white backdrop-blur-md border-gray-200";
  const textColorClass = isHomepage ? "text-white/95" : "text-gray-800";

  return (
    <footer className="absolute bottom-0 left-0 right-0 py-3 text-center text-gray-500 text-sm">
      <div
        className={`${backgroundClass} rounded-lg mx-auto inline-block px-4 py-2 border shadow-lg`}
      >
        <span className={`${textColorClass} font-medium`}>
          v{APP_VERSION} • one small step • @mohammba + @beivers
        </span>
      </div>
    </footer>
  );
};

export default Footer;
