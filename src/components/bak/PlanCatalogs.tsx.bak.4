import React from "react";
import CatalogTable from "./CatalogTable";
import { Course } from "@/types/types";

interface PlanCatalogsProps {
  onScroll?: (scrollTop: number) => void;
  courses?: Course[];
  onCoursesChange?: (courses: Course[]) => void;
}

const PlanCatalogs: React.FC<PlanCatalogsProps> = ({ 
  onScroll, 
  courses = [],
  onCoursesChange 
}) => {
  return (
    <div id="plan-catalogs" className="h-full">
      <CatalogTable 
        onScroll={onScroll}
        courses={courses}
        onCoursesChange={onCoursesChange}
      />
    </div>
  );
};

PlanCatalogs.displayName = "PlanCatalogs";

export default PlanCatalogs;