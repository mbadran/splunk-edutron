import React from 'react';
import { Undo, Redo, Download, FileText, Plus, Upload } from 'lucide-react';

interface HeaderActionsProps {
  onUndo: () => void;
  onRedo: () => void;
  onDownload: () => void;
  canUndo: boolean;
  canRedo: boolean;
}

const HeaderActions = ({
  onUndo,
  onRedo,
  onDownload,
  canUndo,
  canRedo
}: HeaderActionsProps) => {
  const handleQuote = () => {
    alert('Coming soon!');
  };

  const handleNew = () => {
    alert('Coming soon!');
  };

  const handleUpload = () => {
    alert('Coming soon!');
  };

  return (
    <div className="flex items-center gap-4">
      <button
        onClick={onUndo}
        disabled={!canUndo}
        className="flex items-center gap-2 px-4 py-2 bg-orange-300 text-orange-900 rounded-lg hover:bg-orange-400 disabled:bg-gray-300 disabled:text-gray-500 disabled:cursor-not-allowed transition-colors font-medium"
      >
        <Undo className="w-4 h-4" />
        Undo
      </button>
      
      <button
        onClick={onRedo}
        disabled={!canRedo}
        className="flex items-center gap-2 px-4 py-2 bg-orange-300 text-orange-900 rounded-lg hover:bg-orange-400 disabled:bg-gray-300 disabled:text-gray-500 disabled:cursor-not-allowed transition-colors font-medium"
      >
        <Redo className="w-4 h-4" />
        Redo
      </button>

      <button
        onClick={onDownload}
        title="Download"
        className="flex items-center gap-2 px-4 py-2 bg-orange-300 text-orange-900 rounded-lg hover:bg-orange-400 transition-colors font-medium"
      >
        <Download className="w-4 h-4" />
        Download
      </button>

      <button
        onClick={handleQuote}
        title="Generate Quote"
        className="flex items-center gap-2 px-4 py-2 bg-orange-300 text-orange-900 rounded-lg hover:bg-orange-400 transition-colors font-medium"
      >
        <FileText className="w-4 h-4" />
        Quote
      </button>

      {/* Divider */}
      <div className="w-px h-8 bg-gray-300"></div>

      <button
        onClick={handleNew}
        title="New"
        className="flex items-center gap-2 px-4 py-2 bg-orange-300 text-orange-900 rounded-lg hover:bg-orange-400 transition-colors font-medium"
      >
        <Plus className="w-4 h-4" />
        New
      </button>

      <button
        onClick={handleUpload}
        title="Upload"
        className="flex items-center gap-2 px-4 py-2 bg-orange-300 text-orange-900 rounded-lg hover:bg-orange-400 transition-colors font-medium"
      >
        <Upload className="w-4 h-4" />
        Upload
      </button>
    </div>
  );
};

export default HeaderActions;