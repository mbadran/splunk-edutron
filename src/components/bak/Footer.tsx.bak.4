import React from 'react';

interface FooterProps {
  isHomepage?: boolean;
}

const Footer: React.FC<FooterProps> = ({ isHomepage = false }) => {
  const textColorClass = isHomepage ? 'text-white/95' : 'text-white/90';

  return (
    <footer className="absolute bottom-0 left-0 right-0 py-3 text-center text-gray-500 text-sm">
      <div className="bg-white/20 backdrop-blur-md rounded-lg mx-auto inline-block px-4 py-2 border border-white/10 shadow-lg">
        <span className={`${textColorClass} font-medium`}>
          v0.0.1 • By <a href="mailto:mohammba&#64;cisco&#46;com?Subject=Splunk%20EDUTRON" className="text-orange-300 hover:text-orange-200 underline">Mo Badran</a> • <em>Boil 'em, mash 'em, stick 'em in a stew.</em>
        </span>
      </div>
    </footer>
  );
};

export default Footer;