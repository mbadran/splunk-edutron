import type { Config } from "tailwindcss";

const config: Config = {
  content: [
    "./pages/**/*.{js,ts,jsx,tsx,mdx}",
    "./components/**/*.{js,ts,jsx,tsx,mdx}",
    "./app/**/*.{js,ts,jsx,tsx,mdx}",
  ],
  theme: {
    extend: {
      // Splunk Brand Colors
      colors: {
        // Primary Splunk Brand Colors
        'splunk-pink': '#E20082',
        'splunk-orange': '#F99D1C', 
        'splunk-blue': '#0070F3',
        
        // Splunk Gray Scale
        'splunk-gray': {
          'darkest': '#0C1724',
          'darker': '#363C44', 
          'dark': '#969DAA',
          'light': '#D5DCE5',
          'lightest': '#F0F3F7',
        },
        
        // Brand-aware aliases for common usage
        'brand-primary': '#E20082',    // splunk-pink
        'brand-secondary': '#F99D1C',  // splunk-orange
        'brand-accent': '#0070F3',     // splunk-blue
        
        // Background and surface colors
        background: 'var(--background)',
        foreground: 'var(--foreground)',
      },
      
      // Splunk Brand Gradients
      backgroundImage: {
        // Primary brand gradients
        'gradient-splunk-primary': 'linear-gradient(135deg, #F99D1C 0%, #E20082 100%)',
        'gradient-splunk-orange-pink': 'linear-gradient(135deg, #F99D1C 0%, #E20082 100%)',
        'gradient-splunk-pink-blue': 'linear-gradient(135deg, #E20082 0%, #0070F3 100%)',
        
        // Header/hero gradients (purple to black like "Be unstoppably resilient")
        'gradient-splunk-hero': 'linear-gradient(135deg, #6366F1 0%, #4338CA 30%, #312E81 70%, #0C1724 100%)',
        'gradient-splunk-header': 'linear-gradient(135deg, #6366F1 0%, #4338CA 30%, #312E81 70%, #0C1724 100%)',
        
        // Subtle background gradients
        'gradient-splunk-subtle': 'linear-gradient(135deg, #F0F3F7 0%, #D5DCE5 100%)',
        'gradient-splunk-dark': 'linear-gradient(135deg, #363C44 0%, #0C1724 100%)',
      },
      
      // Typography
      fontFamily: {
        sans: ['var(--font-geist-sans)', 'Inter', 'system-ui', 'sans-serif'],
        mono: ['var(--font-geist-mono)', 'Menlo', 'Monaco', 'monospace'],
        display: ['var(--font-geist-sans)', 'Inter', 'system-ui', 'sans-serif'],
        body: ['var(--font-geist-sans)', 'Inter', 'system-ui', 'sans-serif'],
      },
      
      fontWeight: {
        light: '300',
        normal: '400', 
        medium: '500',
        semibold: '600',
        bold: '700',
        extrabold: '800',
      },
      
      // Consistent spacing scale
      spacing: {
        '18': '4.5rem',   // 72px
        '22': '5.5rem',   // 88px
        '26': '6.5rem',   // 104px
        '30': '7.5rem',   // 120px
      },
      
      // Page layout utilities
      maxWidth: {
        'page': '1280px',           // Standard page width
        'page-narrow': '768px',     // Narrow content pages
        'page-wide': '1440px',      // Wide layouts (planner)
      },
      
      // Animation and transitions
      animation: {
        'fade-in': 'fadeIn 0.5s ease-in-out',
        'slide-up': 'slideUp 0.3s ease-out',
        'scale-in': 'scaleIn 0.2s ease-out',
      },
      
      keyframes: {
        fadeIn: {
          '0%': { opacity: '0' },
          '100%': { opacity: '1' },
        },
        slideUp: {
          '0%': { transform: 'translateY(10px)', opacity: '0' },
          '100%': { transform: 'translateY(0)', opacity: '1' },
        },
        scaleIn: {
          '0%': { transform: 'scale(0.9)', opacity: '0' },
          '100%': { transform: 'scale(1)', opacity: '1' },
        },
      },
      
      // Box shadows with brand colors
      boxShadow: {
        'brand': '0 4px 14px 0 rgba(226, 0, 130, 0.15)',
        'brand-lg': '0 10px 25px -3px rgba(226, 0, 130, 0.1), 0 4px 6px -2px rgba(226, 0, 130, 0.05)',
        'orange': '0 4px 14px 0 rgba(249, 157, 28, 0.15)',
      },
    },
  },
  plugins: [
    // Add custom utilities
    function({ addUtilities, theme }: any) {
      const newUtilities = {
        // Page spacing utilities
        '.page-spacing': {
          paddingTop: theme('spacing.8'),
          paddingBottom: theme('spacing.8'),
        },
        '.section-spacing': {
          marginTop: theme('spacing.12'),
          marginBottom: theme('spacing.12'),
        },
        
        // Container utilities
        '.container-constrained': {
          maxWidth: theme('maxWidth.page'),
          marginLeft: 'auto',
          marginRight: 'auto',
          paddingLeft: theme('spacing.6'),
          paddingRight: theme('spacing.6'),
        },
        '.container-narrow': {
          maxWidth: theme('maxWidth.page-narrow'),
          marginLeft: 'auto',
          marginRight: 'auto',
          paddingLeft: theme('spacing.6'),
          paddingRight: theme('spacing.6'),
        },
        '.container-wide': {
          maxWidth: theme('maxWidth.page-wide'),
          marginLeft: 'auto',
          marginRight: 'auto',
          paddingLeft: theme('spacing.6'),
          paddingRight: theme('spacing.6'),
        },
        
        // Brand button utilities
        '.btn-brand': {
          background: 'linear-gradient(135deg, #F99D1C 0%, #E20082 100%)',
          color: 'white',
          fontWeight: theme('fontWeight.bold'),
          padding: `${theme('spacing.3')} ${theme('spacing.6')}`,
          borderRadius: theme('borderRadius.lg'),
          transition: 'all 0.2s ease',
          boxShadow: theme('boxShadow.brand'),
          
          '&:hover': {
            background: 'linear-gradient(135deg, #E8900B 0%, #C70070 100%)',
            boxShadow: theme('boxShadow.brand-lg'),
            transform: 'translateY(-1px)',
          },
          
          '&:active': {
            transform: 'translateY(0)',
          },
        },
        
        // Orange border design system
        '.border-brand': {
          border: `2px solid ${theme('colors.splunk-orange')}`,
          borderRadius: theme('borderRadius.md'),
        },
        '.border-brand-focus': {
          border: `2px solid ${theme('colors.splunk-orange')}`,
          borderRadius: theme('borderRadius.md'),
          '&:focus': {
            borderColor: theme('colors.splunk-pink'),
          },
        },
        '.border-brand-interactive': {
          border: `2px solid ${theme('colors.splunk-orange')}`,
          borderRadius: theme('borderRadius.md'),
          transition: 'border-color 0.2s ease',
          '&:hover': {
            borderColor: theme('colors.splunk-pink'),
          },
        },
      };
      
      addUtilities(newUtilities);
    },
  ],
};

export default config;